export const MAP_LAYER_QUERY_KEYS = {
	MAP_LAYERS: ["mapLayers"] as const,
} as const;

export const MAP_LAYER_PAGE_CONSTANTS = {
	TITLE: "Map Layers",
	DESCRIPTION: "Manage map layer configurations for the interactive map",
} as const;
