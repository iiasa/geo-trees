// React Query Key Constants (for non-generated endpoints only)
export const ROLE_QUERY_KEYS = {
	// Role Management
	ROLES: ["roles"] as const,
} as const;

// Page Constants
export const ROLE_PAGE_CONSTANTS = {
	TITLE: "Roles",
	DESCRIPTION: "Manage system roles and permissions",
} as const;
